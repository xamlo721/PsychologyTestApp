#include "InstrutionTorsonWidget.h"

InstrutionTorsonWidget::InstrutionTorsonWidget(QWidget *parent) : QWidget(parent), uiInstructionTorston(new Ui::InstructionTorstonWidget) {
    uiInstructionTorston->setupUi(this);

}
